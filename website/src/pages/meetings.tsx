import React from 'react';
import Layout from '../components/layout';

const MeetingsPage: React.FC = () => (
  <Layout>
    <p>We meet every Monday in E202, Mr. Mueller's room.</p>
  </Layout>
);

export default MeetingsPage;
